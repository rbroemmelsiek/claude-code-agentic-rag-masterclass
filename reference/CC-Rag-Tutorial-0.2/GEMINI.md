# Gemini CLI context

Follow all rules in **CLAUDE.md** in this directory (stack, services, validation suite, test credentials).

- **Progress:** `PROGRESS.md`
- **Plans:** `.agent/plans/`
- **Slash commands:** `/onboard` and `/build` in `.gemini/commands/`
